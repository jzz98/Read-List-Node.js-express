<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-SgOJa3DmI69IUzQ2PVdRZhwQ+dy64/BUtbMJw1MZ8t5HZApcHrRKUc4W0kG879m7" crossorigin="anonymous">
    <title>Document</title>
</head>

<body>
    <%- include("partials/navbar") %>
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Nombre</th>
                    <th scope="col">Correo</th>
                </tr>
            </thead>
            <tbody>
                <% if(data) {%> 
                    <% for(let i = 0; i < data.length; i++){ %>
                        <tr>
                            <th scope="row"> <%= (i + 1) %></th>
                            <td>
                                <%= data[i].Nombre %>
                            </td>
                            <td>
                                <%= data[i].Correo %>
                            </td>
                        </tr>  
                    <%}  %>
                <% } %>
            </tbody>
        </table>
        <div class="container">
            <div class="d-flex justify-content-end">
                <form action="/agregar-usuario" method="post">
                    <div class="mb-3">
                        <label for="email" class="form-label">Nombre</label>
                        <input type="text" name="Nombre" class="form-control" id="email">
                    </div>
                    <div class="mb-3">
                        <label for="password" class="form-label">correo</label>
                        <input type="text" name="correo" class="form-control" id="password">
                    </div>
                    <button type="submit" class="btn btn-primary">Ingresar</button>
                </form>
            </div>
        </div>

</body>

</html>